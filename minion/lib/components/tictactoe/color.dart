//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class AppColor {
  static Color primaryColor = Color(0XFF212121);
  static Color secondaryColor = Color(0XFF00E5FF);
  static Color accentColor = Colors.white;
}